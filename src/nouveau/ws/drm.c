#include "priv.h"

int
nouveau_ws_drm_new(int fd,
                   struct nouveau_ws_drm **drm)
{
   assert(false);
   return -1;
}

void
nouveau_ws_drm_del(struct nouveau_ws_drm **drm)
{
   assert(false);
}
